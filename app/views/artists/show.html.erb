<h1> Artist </h1>

<h2><%= @artist.name %></h2>
<h2><%= "#{@artist.song_count} songs" %></h2>

<ul><% @artist.songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %></li>
<% end %></ul>
